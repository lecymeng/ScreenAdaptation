buildscript {
  repositories {
    google()
    jcenter()
  }
  dependencies {
    classpath DepLibs.androidPlugin
  }
}

allprojects {
  repositories {
    google()
    jcenter()
  }
}

task clean(type: Delete) {
  delete rootProject.buildDir
}
